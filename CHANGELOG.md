# 更新日志

## [0.0.1] - 2026-05-16

### 新增
- 模组图标（mods 列表与模组详情页显示）
- **ME建筑小帮手**（原版建筑小帮手 + 无线访问点合成）：绑定 AE 后右键建造前自动审计当前模式所需材料，缺料且有样板、身上带无线终端时直接链式拉起 AE2 下单界面
- **ME更替小帮手**（原版更替小帮手 + 无线访问点合成）：同上，作用于更替操作；两者均支持绑定到任意 AE 网络节点，贴图暂用原版
- 在 Building Gadgets 2 材料列表界面添加"从AE下单"按钮
- 通过绑定方块位置实现 AE 网络集成
- 材料列表中实时显示 AE 网络物品数量
- 多个缺失物品的自动合成队列（依次弹窗）
- 入队前自动扣除背包和 AE 网络中已有的物品
- 中文和英文本地化
- 无线终端检测（主手、副手、背包、饰品栏）
- AE 数量查询和合成请求的网络同步
- 全链路诊断日志：在 `config/gadgetsme_network-common.toml` 中开启 `debugLog` 后，右键审计、批量下单、AE2 方案计算/提交、粘贴时网络取料、粘贴执行等全流程会输出 `[GadgetsME/diag]` 标签的详细日志，便于问题排查（默认关闭）
- 本地化：全部聊天提示改用翻译键（zh_cn / en_us），不再硬编码中文文案
- 区分"身上没有无线终端"的情况：缺料但无终端时明确提示且不再误报"请逐一确认下单"，也不进入下单队列；下单中途失去终端会单独提示"下单终止"。携带终端正常下单的提示保持不变

### 修复
- 粘贴时不从 AE 网络正常扣料：BG2 仅在绑定无线访问点时才从网络提取材料，现放宽为任意已绑定的 AE 网络节点（控制器/线缆等），粘贴可正常消耗网络材料与 FE 电量
- 强制粘贴（Shift+右键）现在遵循粘贴锚点，且替换模式不再把被替换方块返还给玩家（与原版粘贴分支一致）
- 队列推进改为固定推迟到下一 tick 执行（`server.execute` 在主线程会内联执行，导致在菜单关闭回调里递归打开下一个界面，引发 StackOverflowError 崩溃）；推进任务按玩家去重，同一 tick 多次关闭只推进一次

### 变更
- **模组 ID 由 `gadgetsme_network` 更名为 `me_building_gadgets`**（显示名：ME Building Gadgets）：资源目录、物品 ID、配方/进阶、网络包与语言键前缀一并迁移；破坏性变更——更名前世界存档中的 `gadgetsme_network:*` 物品将无法识别
- 下单流程改为链式弹出 AE2 原生下单界面：一次触发（右键审计或材料列表"从AE下单"）即把所有缺失且有样板的材料排入队列，确认一个物品的订单后自动弹出到下一个，无需反复唤醒；关闭当前界面即跳过该材料。下单始终由玩家手动确认，绝不自动提交
- 全部下单处理完毕后自动关闭 AE2 顺带打开的合成状态/终端界面，直接回到游戏世界
- 队列打开界面失败时会自动重试而不是丢弃该材料；缺料审计会把"已在合成中的数量"计入可用量，避免对同一材料重复下单
